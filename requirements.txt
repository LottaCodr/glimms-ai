# Dependencies for the all-in-one root image (see the root Dockerfile and
# gateway/).  These are the shared, lightweight requirements under which every
# service imports cleanly and runs its deterministic offline fallbacks.
#
# Deliberately NOT included: torch, transformers, ultralytics, opencv,
# onnxruntime, rembg and pinecone-client.  Every service imports those lazily
# and degrades gracefully when they are absent.  Use the per-service
# requirements under services/<name>/requirements.txt (via docker-compose.yml)
# for the full production images.
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
numpy==1.26.3
Pillow==10.2.0
scipy==1.12.0
boto3==1.34.0
httpx==0.26.0
openai==1.10.0
anthropic==0.18.1
